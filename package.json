{
  "name": "adeptsworn-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "adeptsworn.com — the public site and its editor, on one Cloudflare Worker.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  }
}
